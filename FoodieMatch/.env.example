# =============================================
# FoodieMatch 환경변수 설정
# =============================================
# 이 파일을 복사하여 .env로 저장하고 값을 입력하세요.
# Copy this file to .env and fill in your values.

# =============================================
# 필수 - 데이터베이스 (Neon 또는 로컬 PostgreSQL)
# =============================================
# Neon 예시: postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require
# 로컬 예시: postgresql://postgres:password@localhost:5432/foodiematch_db
DATABASE_URL="postgresql://username:password@localhost:5432/database_name?schema=public"

# =============================================
# 필수 - 세션 보안 (32자 이상 랜덤 문자열)
# =============================================
# 생성 방법: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET="your-secret-key-at-least-32-characters-long"

# =============================================
# 환경 설정
# =============================================
NODE_ENV=development

# =============================================
# Render 배포 시 자동 설정됨 (수동 설정 불필요)
# =============================================
# Render에서 자동으로 설정되는 환경변수:
# RENDER=true  <- 이 변수로 Cookie secure 설정 결정
# PORT=10000   <- Render가 할당하는 포트

# =============================================
# 선택 - Cloudflare R2 스토리지 (클라우드 파일 저장)
# =============================================
# R2를 사용하지 않으면 로컬 uploads 폴더 사용
# Cloudflare Dashboard > R2 > API Tokens에서 발급
# 무료: 10GB 스토리지, 월 10백만 요청

# R2_ACCOUNT_ID=your-cloudflare-account-id
# R2_ACCESS_KEY_ID=your-r2-access-key-id
# R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
# R2_BUCKET_NAME=foodiematch-uploads
# R2_PUBLIC_URL=https://pub-xxxxxxxx.r2.dev

# =============================================
# 선택 - 이메일 SMTP 설정
# =============================================
# Gmail 앱 비밀번호 사용 (2FA 필요)
# Google 계정 > 보안 > 앱 비밀번호에서 발급
# 또는 내부 SMTP 서버 사용

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-char-app-password
SMTP_FROM=noreply@yourcompany.com

# 이메일 발송 전체 ON/OFF (false로 설정하면 모든 이메일 발송 중단)
ENABLE_EMAIL=true

# 이메일 스케줄러 활성화 (true/false)
ENABLE_EMAIL_SCHEDULERS=false

# =============================================
# 앱 URL (이메일 링크에 사용)
# =============================================
# 개발: http://localhost:5001
# 프로덕션 (Render): https://your-app.onrender.com
BASE_URL=http://localhost:5001

# =============================================
# 선택 - TBM 시스템 연동 (해당되는 경우)
# =============================================
VITE_TBM_URL=http://localhost:3001
VITE_API_URL_DEV=http://localhost:5287

# =============================================
# 선택 - 백업 API 키 (로컬 PC 자동 백업용)
# =============================================
# PowerShell 스크립트에서 x-backup-key 헤더로 전달
# 생성 방법: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# BACKUP_API_KEY=your-backup-api-key
